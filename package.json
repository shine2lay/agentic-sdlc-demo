{
  "name": "agentic-sdlc-demo",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build",
    "heroku-postbuild": "cd frontend && rm -rf dist node_modules/.vite && npm install --include=dev && npm run build"
  },
  "engines": {
    "node": "20.x"
  }
}
